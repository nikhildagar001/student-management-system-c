name: Run C Program

on:
  workflow_dispatch:

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    steps:
      - name: Get code
        uses: actions/checkout@v4

      - name: Compile C program
        run: gcc main.c -o main

      - name: Run C program
        run: ./main